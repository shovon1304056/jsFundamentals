
// for each implementation

let loop =  () => {
    const months = ['January', 'February', 'March', 'April'];

        months.forEach(function(month) {
        console.log(month);
        });
};

 loop();