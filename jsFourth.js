let a = [10,20,30];

//for in basically used in object
for (x in a){
    console.log(x); // it will return indexes of array a --- 0,1,2
}

for (x of a){
    console.log(x); // it will return  directly values of array a --- 10,20,30
}


/// Map
// A Map holds key-value pairs where the keys can be any datatype.

// A Map remembers the original insertion order of the keys.

